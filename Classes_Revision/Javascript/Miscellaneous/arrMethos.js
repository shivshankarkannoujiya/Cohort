// every
const numbers = [1, 2, 3, 4, 5, 6];
const isAllEven = numbers.every((num) => num % 2 === 0);

// fill 
const arr = [1, 2, "hitesh", 4, 5, , 9];
arr.fill(10, 1)


const nonEmptyArr = new Array(3)
console.log(nonEmptyArr);
